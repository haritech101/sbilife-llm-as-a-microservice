[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[project]
name = "sbilifeco-paths-llm"
version = "0.2.0"
description = "Paths for LLM microservice"
dependencies = [
    "pydantic>=2.11.5"
]
